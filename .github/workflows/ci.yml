name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: flipslidersand-labs/qa-workflows/.github/workflows/python-test.yml@main
    with:
      coverage-threshold: 90
